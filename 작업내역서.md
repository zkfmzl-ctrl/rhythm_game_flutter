# 왜곡된 리듬 — 작업내역서

이 문서는 다른 컴퓨터나 다른 AI(Claude Code, Cursor 등)에서 이어서 작업할 때 필요한 배경 설명입니다.
프로젝트 코드 자체(`lib/`, `assets/`, `pubspec.yaml` 등)는 이 문서와 같은 폴더에 있습니다.

## 시작하는 법 (다른 컴퓨터)

1. Flutter SDK 설치 (https://docs.flutter.dev/get-started/install) — Windows 기준 3.44.x, Dart 3.12.x에서 작업함.
2. 이 폴더에서 `flutter pub get` 1회 실행.
3. 웹으로 확인: `flutter run -d chrome`
4. 폰(Android)에 설치해서 확인: USB 디버깅 켜고 연결한 뒤 `flutter run -d <기기ID>` 또는 `flutter build apk --release` 후
   `build/app/outputs/flutter-apk/app-release.apk`를 폰에 설치.
5. **모바일 화면 비율 문제 때문에 실제 기기(또는 `flutter run -d <android기기>`)로 반드시 확인하면서 작업할 것.**
   웹 브라우저 창 크기만으로는 실제 폰 화면 비율(약 19.5:9)과 다르게 보일 수 있음 — 이번 세션에서 이걸로 몇 번 헛발질함.

## 프로젝트 구조 핵심 개념

- `lib/main.dart` — 앱 진입점 + 전체 상태(`_RhythmGameHomeState`: coins, gems, equipped 아이템, 퀘스트 수령 여부, 음악 볼륨/음소거 등)를 들고 있는 최상위 위젯.
- `lib/constants.dart` — 전역 상수. `AppTab`, `Difficulty`, `Judge` enum과 플레이 화면 캔버스 크기(`playStageWidth/Height`) 등.
- `lib/widgets/common_widgets.dart` — 공용 위젯: `ContainFrame`(화면 스케일링의 핵심, 아래 설명), `PaperPanel`(모든 박스형 UI의 기본 스타일: 검은 테두리 2.5px + 오프셋 그림자), `PaperToggle`, `CurrencyBar`, `BottomTabs`, `AlertCard`, `BestRecordCard`.
- `lib/screens/*.dart` — 화면별 UI. 전부 `part of '../main.dart'`라서 import 없이 서로의 클래스/전역 변수에 접근 가능.
- `lib/models/*.dart` — 데이터 모델(`Song`, `WearItem`, `Quest`, `RhythmNote`, `GameState` 등)과 픽스처 데이터.

### 화면 스케일링 구조 (제일 중요, 여기서 계속 버그 났음)

이 앱은 **고정 크기 디자인 캔버스를 `FittedBox(BoxFit.contain)`으로 실제 화면에 맞게 축소/확대**하는 방식입니다.

- 홈/캐릭터/꾸미기/퀘스트/상점/설정/곡선택 화면: `ContainFrame` (1300×600 고정) 하나를 **`main.dart`의 `_RhythmGameHomeState.build()`에서 한 번만** 감싸서 배경(`SketchBackground`) + 탭 콘텐츠(`currentScreen()`) + 재화창(`CurrencyBar`) + 하단 탭바(`BottomTabs`)가 **전부 같은 배율로 함께 스케일**되게 만들어 놨습니다.
  (예전엔 `CurrencyBar`/`BottomTabs`가 이 스케일링 밖에 있어서, 창을 줄이면 콘텐츠는 작아지는데 재화창은 그대로라 오히려 커 보이는 버그가 있었음 — 지금은 고쳐짐.)
- 플레이(게임) 화면: `play_screen.dart`가 **별도의 독립된** `FittedBox` + 고정 캔버스(`playStageWidth × playStageHeight`, 지금은 `ContainFrame`과 똑같이 1300×600)를 씁니다. 예전엔 이게 1448×1086(4:3에 가까운 비율)이라 요즘 폰(약 19.5:9, 2.1:1에 가까움)에서 양옆에 시커먼 여백(필러박스)이 크게 생겼던 게 이번에 고친 핵심 버그입니다.

**새 화면을 만들거나 좌표를 하드코딩할 때 반드시 지킬 것**:
- `Stack`에 이미지를 넣을 때 `Positioned.fill`이나 `Positioned(left/top/width/height)`로 명시적으로 채우지 않으면, 이미지가 자기 원본 픽셀 비율대로만 그려지고 캔버스를 다 못 채웁니다 (`play2.png` 배경이 이 버그로 오른쪽이 비어 보였음 — `Image.asset(...)`를 `Positioned.fill(child: Image(...))`로 감싸서 고쳤습니다).
- `Stack`의 기본 `clipBehavior`는 `hardEdge`입니다. 캐릭터 이미지처럼 크기가 변하는 요소는 `BoxFit.contain`을 쓰면 절대 잘리지 않습니다(비율 유지하며 축소될 뿐) — 반대로 잘림 문제가 생기면 `BoxFit.cover`를 의심하거나, 박스 자체가 부모 Stack 높이에 갇혀서 의도치 않게 커지는 중인지 확인하세요.
- `CurrencyBar`, `BottomTabs`처럼 화면 전역에 뜨는 오버레이는 **콘텐츠 쪽 `Padding`에서 그만큼 여백을 미리 비워둬야** 화면 크기가 달라져도 겹치지 않습니다 (`main.dart`의 `18 + 67` 하단 패딩 참고).

## 오늘(이번 세션)까지 진행된 주요 작업

- 홈/곡선택/캐릭터/꾸미기/퀘스트/상점/설정 화면을 목업 이미지 기준으로 재구성.
- 캐릭터 이미지: `charactor/home_character.png`는 **이미 사용자가 알파 채널까지 정상적으로 만들어둔 투명 PNG**입니다(RGBA, 82.1% 투명, 불투명 영역이 캐릭터 실루엣과 정확히 일치). 세션 초중반에 이 파일을 "배경 벽 그림과 겹쳐진 불투명 합성본"으로 잘못 판단해서 밀도 기반 알고리즘으로 재추출을 여러 번 시도했고, 그 과정에서 오히려 없던 각진 자국이 생겨서 사용자가 여러 차례 지적했습니다. 최종적으로 **원본 파일의 투명 콘텐츠 경계로 손실 없이 크롭만 해서(픽셀 재가공 없음) 그대로 사용**하는 것으로 해결 — `assets/sketch/home_character_transparent.png`, `assets/character/character_transparent.png` 둘 다 이 방식으로 생성됨(홈/캐릭터/꾸미기/플레이 화면 전부 이 파일 사용).
  - **교훈**: PNG 투명도를 확인할 땐 이미지 뷰어의 미리보기를 신뢰하지 말고 `PIL`로 알파 채널 자체를 numpy 배열로 열어서(`np.array(im)[:,:,3]`) 직접 확인할 것. 미리보기 도구가 투명 영역을 다른 배경과 합성해서 보여주면 마치 배경 그림이 같이 그려진 것처럼 오인하기 쉬움.
- 폰트: 처음엔 Windows `batang.ttc`에서 궁서체를 추출해 썼으나 렌더링 문제(가짜 볼드 합성으로 인한 글자 깨짐)가 반복돼서, 최종적으로 **Noto Sans KR 가변 폰트**(Google Fonts, `assets/fonts/NotoSansKR.ttf`)로 교체. 실제 여러 굵기 외곽선을 가진 정식 폰트라 이 문제가 구조적으로 없습니다.
- 배경음악: `assets/music/background.mp3`를 `audioplayers` 패키지로 앱 시작 시부터 반복 재생, 설정의 볼륨 슬라이더 및 신규 추가한 "전체 음소거" 토글과 연동.
- 플레이 트랙 전환: 게임 시작(`startGame`) 시 `_bgm.pause()` 후 `assets/music/distorted_rhythm.wav`("play 곡")를 `_playTrack`으로 재생, 게임 종료(`exitGame`) 시 `_playTrack.stop()` 후 `_bgm.resume()`으로 메뉴 음악 복귀. 음소거/볼륨은 두 플레이어 모두에 동시 적용.
- 옷장(꾸미기) 시스템: `set/item` 폴더에 있던 실제 분할 아이템 그림(상의/하의/신발 각 7종, 손아이템→장신구로 재활용 7종)을 반영. 슬롯별로 독립적으로 장착 상태를 관리하도록 상태 모델을 `Map<String slot, String itemName>`으로 변경.
  - **한계**: 헤어/얼굴 슬롯은 아이템별 개별 그림이 없어서 아직 카테고리 공용 아이콘을 씁니다. 또한 옷을 갈아입어도 캐릭터 실제 이미지가 바뀌지는 않습니다(아이템별로 "그 옷을 입은 캐릭터 전신 그림"이 없기 때문 — `character-tops_XX.png`류는 상의별로 있지만 하의/신발과 조합이 안 됨).
- 게임플레이(플레이 화면)를 사용자가 준 스펙 이미지 기준으로 5레인 낙하식 → **8방향 판정 링(캐릭터를 중심으로 원형 배치)** 구조로 전면 재작성. 기본(EASY/NORMAL) 6개 판정판(상단 3+하단 3), HARD/EXTREME은 좌우 손 판정판 2개 추가로 8개.
  - **단순화한 부분**: 롱노트는 스펙처럼 두 판정판을 연결해서 유지하는 대신 한 링에서 한 번 탭으로 판정. 바닥 노트의 "점프"도 별도 스와이프 동작이 아니라 하단 링 탭으로 대체.
- 이번 세션 마지막에 **실제 연결된 안드로이드 폰(adb)으로 직접 스크린샷을 찍어 검증**했고, 그 결과로 플레이 화면 배경이 안 채워지던 결정적 버그(`Positioned.fill` 누락)를 찾아 고쳤습니다. 앞으로도 레이아웃 관련 버그는 브라우저보다 실기기 스크린샷이 훨씬 정확합니다:
  ```bash
  adb shell screencap -p /sdcard/screen.png
  adb pull //sdcard/screen.png ./screen.png   # 슬래시 두 개로 시작해야 git-bash 경로 변환을 피함
  ```

- 판정 링 보드에서 노트가 없을 때 캐릭터가 보라색(넉백)으로 보이던 버그 수정: `GameState.knockbackUntil` 기본값이 `0`이라 게임 시작 시각(`elapsed`가 `-800`부터 시작)보다 작아 항상 "방금 맞음" 상태로 오판되던 것을, 기본값을 `-100000`으로 바꿔 해결.
- 노트 패턴 다양화: `buildChart()`가 고정 순환 대신 시드 기반 `math.Random`으로 링 순서를 셔플하고 롱노트 배치도 랜덤화하도록 재작성.
- 판정(퍼펙트/그레이트/굿/미스) 표시(`JudgementLayer`) 위치를 진행바 아래로, 글자 크기를 키워서 더 눈에 띄게 조정.
- 모든 화면의 상단 탭(퀘스트/상점/꾸미기)을 `Expanded`로 감싸 균일한 폭으로 통일. 설정 화면의 각 행을 `_SettingRow` 공용 위젯(높이 44, 좌측 정렬)으로 통일. 옷장/상점 아이템 그리드 셀 크기(`mainAxisExtent`)를 120으로 통일. 캐릭터 선택 화면의 대표 캐릭터 카드 높이를 왼쪽 정보 패널과 맞춤.
- 퀘스트 화면의 "모두 받기" 버튼을 리스트 하단 전체 폭 버튼으로 이동.
- **배경이 화면 한쪽에서 잘려 보이던 버그 수정**: 카메라 펀치홀/커브드 엣지가 있는 기기에서 `SafeArea`가 한쪽으로만 여백을 주는 바람에, 배경까지 그 `SafeArea` 안에 같이 있어서 화면 세로 전체에 걸쳐 흰 여백이 생겼음(장식일 뿐인 배경은 굳이 안전영역 안에 있을 필요가 없음). `main.dart`에서 배경(`_AppBackdrop`, `common_widgets.dart`)을 `SafeArea` 바깥으로 빼서 실제 물리 화면 전체를 항상 채우도록 하고, 탭 가능한 UI(콘텐츠/재화창/하단탭)만 `SafeArea` 안에 남겼음.
- **플레이 화면 곡 제목/재생 길이 수정**: 실제 재생되는 오디오 파일(`distorted_rhythm.wav`, 93.48초)이 화면에 표시되던 제목("왜곡된 리듬" — 이건 게임 자체 이름)과 달랐고, 노트 차트도 42개 고정이라 14초 만에 끝나버려서 93초짜리 곡이 끝까지 재생되기 전에 게임이 먼저 끝났음.
  - `song_fixtures.dart`의 1번곡 제목을 실제 파일명 기준 "끝을 잃어버린 밤"으로 수정, `Song`에 `durationMs` 필드 추가(93480ms).
  - `buildChart()`가 고정 42개 대신 `durationMs`에 맞춰 노트 개수를 계산해서 곡 끝까지 채우도록 변경.
  - `GameState`에 `durationMs`를 들려보내고, 게임 종료 조건을 "마지막 노트+여유시간"이 아니라 "경과시간이 `durationMs`에 도달"로 변경 — 이제 게임은 항상 노래가 끝날 때까지 진행됨.
  - 플레이 화면 상단 진행시간 표시("0:07 / 1:34" 같은 하드코딩 텍스트)를 `game.durationMs` 기반 실제 값으로 계산.
- **설정 화면 좌측 정렬**: 다른 2단 레이아웃 화면(캐릭터/꾸미기)을 흉내 내려고 왼쪽에 빈 `Expanded(flex: 40)` 스페이서를 넣어뒀던 게 원인 — 설정은 원래 1단 레이아웃이라 그냥 리스트만 두면 됐음. 스페이서 제거하고 `ListView`가 전체 폭을 쓰도록 수정(`settings_screen.dart`).
- **설정 화면에서 재화창(코인/보석) 숨김**: `main.dart`에서 `CurrencyBar`를 `if (tab != AppTab.settings)`로 감싸서 설정 탭에서는 표시 안 되게 함.
- **플레이 화면 몰입모드**: 안드로이드 3버튼 내비게이션 바가 판정 링/노트 위에 겹쳐서 조작을 방해하던 문제. `startGame()`에서 `SystemChrome.setEnabledSystemUIMode(SystemUiMode.immersiveSticky)`로 전체화면 전환, `exitGame()`에서 `SystemUiMode.edgeToEdge`로 복원.

## 알려진 한계 / 다음에 손볼 만한 것

1. 옷장 아이템 착용이 캐릭터 실제 모습에 반영 안 됨 (위 설명 참고).
2. 헤어/얼굴 슬롯은 아이템별 그림 없음.
3. 롱노트/점프가 스펙보다 단순화됨.
4. 상점/퀘스트 데이터가 대부분 하드코딩된 픽스처라 실제 저장/영속성 없음(앱 재시작하면 초기화).

## 참고: 지금까지 대화에서 반복됐던 사용자 피드백 패턴

- "박스/캐릭터가 화면 크기에 안 맞게 커진다" → 거의 항상 "고정 픽셀 크기 + 전역 스케일링 밖에 있는 위젯" 조합이 원인이었음. 새로 뭔가 화면에 고정 배치할 땐 반드시 `ContainFrame`/`playStageWidth,Height` 스케일 좌표계 **안에서** 좌표를 잡을 것.
- 굵은 텍스트가 깨져 보인다 → 폰트가 해당 굵기의 실제 외곽선이 없어서 가짜 볼드 합성이 일어나는 게 원인이었음. 새 폰트 추가 시 여러 굵기 파일이 있는지 확인.
